package Interfaces;

public interface Atacadora {
    /**
     * Metodo que permite a una pieza realizar un movimiento fuera de lo normal
     * @param fila a la que se quiere mover
     * @param columna a la que se quiere mover
     */
    public void ataque(int fila, int columna);
}
